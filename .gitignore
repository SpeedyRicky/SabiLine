node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example
*.tsbuildinfo
# Local persistence for scheduled Twilio appointment reminders (see
# src/services/reminder/reminderScheduler.ts) — never committed.
data/
# Generated at build time by esbuild (bundles server.ts for Vercel's
# serverless runtime) — see the "build" script in package.json.
api/index.js
api/index.js.map
